project(":freedylang") {
    apply plugin: "java-library"
    ext.module = () ->{}
    dependencies {
        api project(":common")
    }
}

project(":mcfreedylang") {
    apply plugin: "java-library"
    ext.module = () ->{}
    dependencies {
        api project(":freedylang")
    }
}